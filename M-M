#include <bits/stdc++.h>
using namespace std;

vector<int> v;

int main()
{
    int n, i, a;
    while (cin >> n && n != 0)
    {
        v.clear();
        for (i = 0; i < n; i++)
        {
            cin >> a;
            v.push_back(a);
        }
        sort(v.begin(), v.end());

        for (i = 0; i < n; i++)
        {
            cout << v[i];
            if(i != n - 1)
            cout << " ";
        }
        cout << endl;
    }
}
